#!/bin/bash

FROM_LBIN="/home/numen/.local/bin"
TO_BIN="/usr/local/bin"

function gen_link()
{
    sudo ln -v "$FROM_LBIN/$1" "$TO_BIN"
}
# sudo rm -v "$TO_BIN/*"

gen_link "bacup"
gen_link "easylog"

